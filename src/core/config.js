/**
 * 調整値。
 *
 * 距離と標高は実測そのままなので、遊べる長さに収めるのは時間の圧縮で行う。
 * TIME_SCALE=3 のとき、実時間 1 秒がゲーム内 3 秒に相当する。
 * 飛脚の速度は「ゲーム内秒あたりの実距離」で与えてあるので、
 * 走破に要する *ゲーム内* 時間は史実どおり(二里を約半刻)になり、
 * プレイヤーが座っている実時間だけが 1/3 に縮む。
 */
export const TIME_SCALE = 3;

/** 早駆け(開発・実演用)。押している間さらに速く進む。 */
export const FAST_FORWARD = 5;

export const SPEED = {
  /** 歩き 5.8 km/h — S を押して緩めたとき */
  walk: 1.6,
  /**
   * 駆け足 10.8 km/h — 手を離したときの足取り。
   *
   * 走りと歩きの二段しか無いと、息を戻す手立てが歩くことだけになる。
   * それでは息が切れて勝手に歩かされるのと変わらず、自分から緩める
   * 意味が生まれない。あいだに一段置くと、息を戻しながらも進めるので、
   * 「切れる前に緩める」ことがはじめて得になる。
   */
  jog: 3.0,
  /** 並の走り 16 km/h — 飛脚の巡航 */
  run: 4.5,
  /** 全力 22 km/h — 息が続かない */
  sprint: 6.2,
  /** 息が切れたときの足 4.1 km/h。自分から緩めた駆け足より遅い。 */
  spent: 1.15,
  /** 加速・減速 (m/s^2) */
  accel: 3.2,
  brake: 5.0,
  /** 左右への移動 (m/s)。倒し具合を掛けた値が実際の速さになる。 */
  strafe: 3.4,
  /**
   * 横移動の追従の速さ。
   * 入力にそのまま追従させると、指が触れた瞬間に全速で横へ飛ぶ。
   * 少し遅らせると、軽く触れたときの動き出しが穏やかになる。
   */
  strafeResponse: 9,
};

/**
 * 息の配分。
 *
 * 飛脚は走るのが商売なので、平らな道の巡航はほぼ持続できる。
 * 息を持っていくのは坂と全力疾走で、そのぶん下りでは息が整う。
 * 走り方の判断はこの三つの差から生まれる。街道の勾配は実測なので、
 * どこで息を使いどこで歩くかは、その街道の地形そのものが決める。
 *
 * 回復を速くすると、息は「切れたら少し歩けば済むもの」になって
 * 配分を考える意味が消える。歩いて戻すのに実時間で十数秒かかる
 * くらいにしておくと、坂の手前で貯めるかどうかが本当の判断になる。
 */
export const STAMINA = {
  max: 100,
  /** 毎ゲーム内秒の消耗 */
  runDrain: 0.12,
  sprintDrain: 4.5,
  /** 駆け足で戻る量。走りを緩めたときの主な回復手段。 */
  jogRecover: 1.9,
  /** 歩き・停止時の回復 */
  walkRecover: 2.8,
  idleRecover: 3.4,
  /**
   * 息が切れているあいだの回復。自分から緩めたときより遅い。
   * ここで差を付けないと、切れるまで走ってから休むのが最善になってしまう。
   */
  spentRecover: 1.3,
  /**
   * これ以下の勾配は平らとみなす。
   * 千分の五は百メートルで五十センチ。飛脚が息を切らす坂ではない。
   */
  gradeFree: 5,
  /** 勾配 1‰ あたりの追加消耗 (上りのみ) */
  gradePenalty: 0.032,
  /**
   * 下り 1‰ あたりの消耗の戻し。
   * 坂を駆け下りるあいだは息が整う。上りで使った分をここで取り返せるので、
   * 峠のある街道は「登りきるまで保たせる」遊びになる。
   */
  descentRelief: 0.03,
  /** 息が切れると走れなくなる。この値まで戻れば再び走れる。 */
  exhaustedUntil: 45,
};

export const CAMERA = {
  lookAhead: 9.0,
};

/**
 * 視点。
 *
 * 遠見だけだと自分がどこを走っているのか他人事になる。目線まで寄せると
 * 軒の低さや溝の幅が身体の寸法で分かるようになるので、こちらを既定にする。
 *
 *   distance   … 飛脚の背後へ下がる距離 (m)。0 なら本人の目
 *   height     … 路面からのカメラの高さ (m)
 *   lookHeight … 見る先の高さ (m)
 *   lag        … 追従の緩さ。大きいほど身体に固く付く
 *   bob        … 足に合わせた上下の揺れ (m)
 *   uFollow    … 横ずれをどれだけ追うか。目線は本人なので 1
 */
export const VIEWS = [
  {
    id: 'me',
    name: '目線',
    distance: 0,
    height: 1.63,
    lookHeight: 1.63,
    fov: 74,
    lag: 22,
    bob: 0.055,
    uFollow: 1,
    firstPerson: true,
  },
  {
    id: 'kata',
    name: '肩越し',
    distance: 3.3,
    height: 2.1,
    lookHeight: 1.5,
    fov: 66,
    lag: 8,
    bob: 0.022,
    uFollow: 0.8,
  },
  {
    id: 'tooku',
    name: '遠見',
    distance: 7.4,
    height: 3.0,
    lookHeight: 1.3,
    fov: 62,
    lag: 4.5,
    bob: 0,
    uFollow: 0.5,
  },
];

/**
 * よそ見。
 * 走りながら首を回す。飛脚を軸に camera が回り込むので、
 * 横を向けば海や城が正面に来る。手を離せば進行方向へ戻る。
 */
export const LOOK = {
  /** 左右に振れる角度 (度) */
  maxYaw: 135,
  /** 上下に振れる角度 (度) */
  maxPitch: 26,
  /** キー操作の速さ (度/秒) */
  keySpeed: 150,
  /** マウス 1px あたりの角度 (度) */
  mouseYaw: 0.28,
  mousePitch: 0.2,
  /** 指 1px あたりの角度 (度)。親指の可動域は狭いので大きめに取る。 */
  touchYaw: 0.45,
  touchPitch: 0.3,
  /**
   * 首の動きのばね定数。臨界減衰させるので、振り始めと戻り終わりで
   * 速度が 0 になる。指数減衰だと「最初だけ速くて後は這う」戻り方になり、
   * 前を向き直る瞬間が急に感じられる。
   * 収まるまでおよそ 4/√k 秒。
   */
  stiffness: 13,
  /** 横を向いているあいだは霞を遠くまで払う */
  fogFarLookaway: 1300,
};

export const FOG = { near: 40, far: 340 };

/** 沿道の生成に使う種。変えると町並みが変わる。 */
export const WORLD_SEED = 1603;
